package Elements;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;

public class Text extends AbstractElement {
    public Text(final WebDriver driver, final By locator) {
        super(driver, locator);
    }
}
