package Elements;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;

public class Ul extends AbstractElement {
    public Ul(final WebDriver driver, final By locator) {
        super(driver, locator);
    }
}
